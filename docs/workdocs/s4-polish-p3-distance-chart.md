# S4-Polish P3 — 原距离预览折线图（已废弃）

> 状态：✅ 已并入 `s4-polish-s4-vol.md`，不再单独派发。
> 原“距离预览折线图”已升级为 **相机高度衰减编辑器**（可拖拽两点 + 三快速预设按钮），由 S4-Vol 功能块实现。
> 纯函数 `DistancePreview.cs` 继续保留，线性 min→max 语义不变。
> 请勿按本文件旧内容派发 worker。
